{
  "name": "@route-core/desktop",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "bun run build && electrobun dev",
    "build": "bun install && electrobun build",
    "build:release": "electrobun build --release",
    "dev": "electrobun dev --watch"
  },
  "dependencies": {
    "@route-core/core": "*",
    "electrobun": "^1.16.0",
    "better-sqlite3": "^11.0.0",
    "express": "^4.21.0",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/express": "^4.17.21",
    "@types/cors": "^2.8.17",
    "@types/better-sqlite3": "^7.6.9",
    "typescript": "^5.4.0"
  }
}
